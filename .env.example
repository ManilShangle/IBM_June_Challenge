# Which backend to use for Granite inference: replicate | watsonx | huggingface | ollama | langflow
GRANITE_BACKEND=replicate

# --- Langflow (live visual orchestration backend) ---
# Start Langflow, import flows/var_predictor_flow.json, then set:
LANGFLOW_API_URL=http://localhost:7860
LANGFLOW_FLOW_ID=1faee69d-d060-4a1d-b573-001ca4180798

# --- Video analysis ---
# Number of frames to extract from uploaded video clips (default 5)
VIDEO_FRAMES=5

# --- Replicate (primary - lowest friction, GitHub login only) ---
# 1. Sign up at replicate.com with GitHub
# 2. Claim free credit: https://replicate.fyi/ibm
# 3. Create a token at replicate.com/account/api-tokens
REPLICATE_API_TOKEN=
REPLICATE_MODEL=ibm-granite/granite-3.3-8b-instruct

# --- watsonx.ai (alternative, official IBM path) ---
WATSONX_API_KEY=
WATSONX_PROJECT_ID=
WATSONX_URL=https://us-south.ml.cloud.ibm.com
WATSONX_MODEL_ID=ibm/granite-3-8b-instruct

# --- Hugging Face Inference API (fallback) ---
HF_API_TOKEN=
HF_MODEL_ID=ibm-granite/granite-3.1-8b-instruct

# --- Ollama (local fallback) ---
OLLAMA_MODEL=granite3.1-dense:8b
OLLAMA_HOST=http://localhost:11434
