# Demo Clips

MP4 files are gitignored (too large). Download locally with:

```
yt-dlp -f "worst[ext=mp4]/worst" -o "%(title)s.%(ext)s" <youtube-url>
```

Included when the repo is cloned from the release or demo package.
